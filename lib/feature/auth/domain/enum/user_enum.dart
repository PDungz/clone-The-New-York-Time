enum UserRole {
  admin,
  user,
  moderator,
  editor,
}

enum UserStatus {
  active,
  inactive,
  suspended,
  locked,
  banned,
  deleted,
}

enum SubscriptionType {
  free,
  basic,
  premium,
  enterprise,
}

enum SubscriptionStatus {
  active,
  inactive,
  expired,
  cancelled,
  suspended,
}